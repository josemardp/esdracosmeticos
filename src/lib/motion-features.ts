// Motor de animação do framer-motion, carregado depois da página (ver LazyMotion no App.tsx).
// domMax porque o catálogo e o carrinho usam animação de layout.
import { domMax } from "framer-motion";
export default domMax;
